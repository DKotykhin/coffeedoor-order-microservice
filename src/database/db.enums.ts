export enum LanguageCode {
  UA = 'UA',
  EN = 'EN',
}

export enum OrderStatus {
  NEW = 'NEW',
  IN_PROGRESS = 'IN_PROGRESS',
  DONE = 'DONE',
  CANCELED = 'CANCELED',
}

export enum DeliveryWay {
  DELIVERY = 'DELIVERY',
  PICKUP = 'PICKUP',
}
